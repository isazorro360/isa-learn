rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /past-questions/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email in ['admin@isaitech.com'];
    }
  }
}
